{
  "name": "practice-room-chat-backend",
  "version": "1.0.0",
  "description": "Backend API for Practice Room Chat iOS app",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=musicTheoryChat",
    "deploy": "gcloud functions deploy musicTheoryChat --runtime nodejs20 --trigger-http --allow-unauthenticated --set-env-vars API_KEY=$API_KEY --region us-central1"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "google-auth-library": "^9.0.0",
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}